plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
}

def copyVocabulary = tasks.register('copyVocabularyJson', Copy) {
    def src = rootProject.file('../web/data/vocabulary.json')
    from src
    into layout.projectDirectory.dir('src/main/assets')
    onlyIf { src.exists() }
}

def copyEssays = tasks.register('copyEssaysJson', Copy) {
    def src = rootProject.file('../web/data/essays.json')
    from src
    into layout.projectDirectory.dir('src/main/assets')
    onlyIf { src.exists() }
}

def copyReading = tasks.register('copyReadingContentJson', Copy) {
    def src = rootProject.file('../web/data/reading_content.json')
    from src
    into layout.projectDirectory.dir('src/main/assets')
    onlyIf { src.exists() }
}

def copyEssayAudio = tasks.register('copyEssayAudio', Copy) {
    def src = rootProject.file('../web/data/audio/essays')
    from src
    into layout.projectDirectory.dir('src/main/assets/audio/essays')
    include '**/*.wav'
    onlyIf { src.exists() }
}

def copyDaofaText = tasks.register('copyDaofaText', Copy) {
    def src = rootProject.file('../web/data/daofa_reference.txt')
    from src
    into layout.projectDirectory.dir('src/main/assets')
    onlyIf { src.exists() }
}

def copyDaofaStructure = tasks.register('copyDaofaStructure', Copy) {
    def src = rootProject.file('../web/data/daofa_structure.txt')
    from src
    into layout.projectDirectory.dir('src/main/assets')
    onlyIf { src.exists() }
}

def copyReadingHighFreq = tasks.register('copyReadingHighFreq', Copy) {
    def src = rootProject.file('../web/data/reading_high_freq.json')
    from src
    into layout.projectDirectory.dir('src/main/assets')
    onlyIf { src.exists() }
}

def copyMc688 = tasks.register('copyMc688', Copy) {
    def src = rootProject.file('../web/data/mc688_21day.json')
    from src
    into layout.projectDirectory.dir('src/main/assets')
    onlyIf { src.exists() }
}

def copyReadingSkills = tasks.register('copyReadingSkills', Copy) {
    def src = rootProject.file('../web/data/reading_skills_zhongkao.json')
    from src
    into layout.projectDirectory.dir('src/main/assets')
    onlyIf { src.exists() }
}

def copyDaofaPastExams = tasks.register('copyDaofaPastExams', Copy) {
    def src = rootProject.file('../web/data/daofa_past_exams.json')
    from src
    into layout.projectDirectory.dir('src/main/assets')
    onlyIf { src.exists() }
}

def copyChineseZhongkao = tasks.register('copyChineseZhongkao', Copy) {
    def src = rootProject.file('../web/data/chinese_zhongkao.json')
    from src
    into layout.projectDirectory.dir('src/main/assets')
    onlyIf { src.exists() }
}

def copyChineseExamImages = tasks.register('copyChineseExamImages', Copy) {
    def srcDir = rootProject.file('../web/data/chinese2025')
    from srcDir
    into layout.projectDirectory.dir('src/main/assets/chinese2025')
    include '**/*'
    onlyIf { srcDir.exists() }
}

def copyMathZhongkao = tasks.register('copyMathZhongkao', Copy) {
    def src = rootProject.file('../web/data/math_zhongkao.json')
    from src
    into layout.projectDirectory.dir('src/main/assets')
    onlyIf { src.exists() }
}

def copyMathExamImages = tasks.register('copyMathExamImages', Copy) {
    def srcDir = rootProject.file('../web/data/math2025')
    from srcDir
    into layout.projectDirectory.dir('src/main/assets/math2025')
    include '**/*'
    onlyIf { srcDir.exists() }
}

def copyPhysicsZhongkao = tasks.register('copyPhysicsZhongkao', Copy) {
    def src = rootProject.file('../web/data/physics_zhongkao.json')
    from src
    into layout.projectDirectory.dir('src/main/assets')
    onlyIf { src.exists() }
}

def copyPhysicsExamImages = tasks.register('copyPhysicsExamImages', Copy) {
    def srcDir = rootProject.file('../web/data/physics2025')
    from srcDir
    into layout.projectDirectory.dir('src/main/assets/physics2025')
    include '**/*'
    onlyIf { srcDir.exists() }
}

def copyPhysicsStructure = tasks.register('copyPhysicsStructure', Copy) {
    def src = rootProject.file('../web/data/physics_beijing_structure.json')
    from src
    into layout.projectDirectory.dir('src/main/assets')
    onlyIf { src.exists() }
}

def copyMathStructure = tasks.register('copyMathStructure', Copy) {
    def src = rootProject.file('../web/data/math_beijing_structure.json')
    from src
    into layout.projectDirectory.dir('src/main/assets')
    onlyIf { src.exists() }
}

def copyChineseStructure = tasks.register('copyChineseStructure', Copy) {
    def src = rootProject.file('../web/data/chinese_beijing_structure.json')
    from src
    into layout.projectDirectory.dir('src/main/assets')
    onlyIf { src.exists() }
}

def copyDaofaBeijingStructure = tasks.register('copyDaofaBeijingStructure', Copy) {
    def src = rootProject.file('../web/data/daofa_beijing_structure.json')
    from src
    into layout.projectDirectory.dir('src/main/assets')
    onlyIf { src.exists() }
}

def copyEnglishStructure = tasks.register('copyEnglishStructure', Copy) {
    def src = rootProject.file('../web/data/english_beijing_structure.json')
    from src
    into layout.projectDirectory.dir('src/main/assets')
    onlyIf { src.exists() }
}

def copyPeSportsStructure = tasks.register('copyPeSportsStructure', Copy) {
    def src = rootProject.file('../web/data/pe_beijing_structure.json')
    from src
    into layout.projectDirectory.dir('src/main/assets')
    onlyIf { src.exists() }
}

tasks.named('preBuild').configure { dependsOn copyVocabulary, copyEssays, copyReading, copyEssayAudio, copyDaofaText, copyDaofaStructure, copyReadingHighFreq, copyMc688, copyReadingSkills, copyDaofaPastExams, copyChineseZhongkao, copyChineseExamImages, copyMathZhongkao, copyMathExamImages, copyPhysicsZhongkao, copyPhysicsExamImages, copyPhysicsStructure, copyMathStructure, copyChineseStructure, copyDaofaBeijingStructure, copyEnglishStructure, copyPeSportsStructure }

android {
    namespace 'com.cltf.aienglish'
    compileSdk 34

    defaultConfig {
        applicationId "com.cltf.aienglish"
        minSdk 26
        targetSdk 34
        versionCode 1
        versionName "1.0.0"
        // 模拟器访问本机代理用 10.0.2.2；真机本机代理可改为 127.0.0.1 后重新编译
        buildConfigField "String", "AI_PROXY_HOST", "\"10.0.2.2\""
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = '17'
    }

    buildFeatures {
        compose true
        buildConfig true
    }
    composeOptions {
        kotlinCompilerExtensionVersion '1.5.14'
    }
}

dependencies {
    implementation platform('androidx.compose:compose-bom:2024.06.00')
    implementation 'androidx.compose.ui:ui'
    implementation 'androidx.compose.ui:ui-graphics'
    implementation 'androidx.compose.ui:ui-tooling-preview'
    implementation 'androidx.compose.material3:material3'
    implementation 'androidx.compose.material:material-icons-extended'
    implementation 'androidx.activity:activity-compose:1.9.0'
    implementation 'androidx.lifecycle:lifecycle-runtime-ktx:2.8.4'
    implementation 'androidx.lifecycle:lifecycle-viewmodel-compose:2.8.4'
    implementation 'androidx.lifecycle:lifecycle-runtime-compose:2.8.4'
    implementation 'androidx.lifecycle:lifecycle-viewmodel-ktx:2.8.4'

    implementation 'org.jetbrains.kotlinx:kotlinx-coroutines-android:1.8.1'

    implementation 'com.google.android.material:material:1.12.0'
    implementation 'androidx.core:core-ktx:1.13.1'

    implementation 'com.squareup.okhttp3:okhttp:4.12.0'
    implementation 'com.google.code.gson:gson:2.11.0'

    implementation 'androidx.datastore:datastore-preferences:1.1.1'

    debugImplementation 'androidx.compose.ui:ui-tooling'
    debugImplementation 'androidx.compose.ui:ui-test-manifest'
}
